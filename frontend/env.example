# frontend/.env.example
# Copy this to ".env" for local dev, or set these in Vercel's dashboard for production

# Your backend URL on Render (leave empty for local dev — Vite proxy handles it)
# In production on Vercel, set this to: https://your-app-name.onrender.com/api
VITE_API_URL=
